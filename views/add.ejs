<%- include('partials/header', { title: 'Add New Vehicle', page: 'add' }) %>

<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
    <h2 style="color: #2c3e50; font-size: 2rem;">Add New Vehicle</h2>
    <a href="/" class="btn">← Back to Inventory</a>
</div>

<div style="max-width: 600px; margin: 0 auto;">
    <form method="POST" action="/vehicles" style="background: #f8f9fa; padding: 30px; border-radius: 10px;">
        <div class="form-group">
            <label for="brand">Brand *</label>
            <input type="text" id="brand" name="brand" required
                   placeholder="e.g., Maruti Suzuki, Hyundai, Tata">
        </div>

        <div class="form-group">
            <label for="name">Vehicle Name *</label>
            <input type="text" id="name" name="name" required
                   placeholder="e.g., Maruti Suzuki Swift 2024">
        </div>

        <div class="form-group">
            <label for="model">Model *</label>
            <input type="text" id="model" name="model" required
                   placeholder="e.g., Swift, Creta, Nexon">
        </div>

        <div class="form-group">
            <label for="price">Price (INR) *</label>
            <input type="number" id="price" name="price" required min="0" step="1"
                   placeholder="e.g., 1200000">
        </div>

        <div class="form-group">
            <label for="image">Image URL</label>
            <input type="url" id="image" name="image" 
                   placeholder="https://example.com/image.jpg">
            <small style="color: #7f8c8d; display: block; margin-top: 5px;">
                Leave empty to use placeholder image
            </small>
        </div>

        <div class="form-group">
            <label for="desc">Description</label>
            <textarea id="desc" name="desc" rows="4" 
                      placeholder="Enter vehicle description, features, etc."></textarea>
        </div>

        <div style="display: flex; gap: 15px; justify-content: flex-end;">
            <a href="/" class="btn" style="background: #95a5a6;">Cancel</a>
            <button type="submit" class="btn btn-success">Add Vehicle</button>
        </div>
    </form>
</div>

<div style="margin-top: 40px; padding: 20px; background: #e8f4fd; border-radius: 10px; border-left: 4px solid #3498db;">
    <h4 style="color: #2c3e50; margin-bottom: 10px;">💡 Tips</h4>
    <ul style="color: #34495e; line-height: 1.6;">
        <li>Fields marked with * are required</li>
        <li>Use descriptive names to make vehicles easy to identify</li>
        <li>Include high-quality image URLs for better presentation</li>
        <li>Add detailed descriptions to help with vehicle selection</li>
    </ul>
</div>

<%- include('partials/footer') %>
